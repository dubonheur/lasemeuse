
<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
	<meta name="description" content="Responsive Admin &amp; Dashboard Template based on Bootstrap 5">
	<meta name="author" content="AdminKit">
	<meta name="keywords" content="adminkit, bootstrap, bootstrap 5, admin, dashboard, template, responsive, css, sass, html, theme, front-end, ui kit, web">

	<link rel="preconnect" href="{{asset('assets/https://fonts.gstatic.com')}} ">
	<link rel="shortcut icon" href="{{asset('assets/ img/icons/icon-48x48.png')}}" />

	<link rel="canonical" href="https://demo-basic.adminkit.io/" />

	<title>Complexe scolaire la semeuse</title>

	<link href="{{asset('assets/css/app.css')}} " rel="stylesheet">
	<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet">

	


</head>
<div class="wrapper">
    {% include "menu/nav_footer/nav.html.twig" %}


    <main class="content">
        

		<div class="container-fluid p-0">

			<h1 class="h3 mb-3"><strong>Listes </strong>des Utilisateurs</h1>
		
			<div class="row">
		
				<div class="card">
					<div class="card-body">
		
						<a href="{{ path('ajouter_personne') }}"><img src="{{ asset('assets/img/icons/ajouter.png') }}"></a></br></br>
		
						<table class="table table-hover tpedable-stri">
							<thead>
								<tr>
									<th>Email</th>
									<th>Rôles</th>
									<th>Nom</th>
									<th>Prénom</th>
									<th>Téléphone</th>
									<th>Sexe</th>
									<th>Nationalité</th>
									<th>Profession</th>
									<th>Photo</th>
									<th>Actions</th>
								</tr>
							</thead>
							<tbody>
								{% for user in users %}
									<tr>
										<td>{{ user.email }}</td>
										<td>
											{% for role in user.roles %}
												{% if role == "ROLE_PARENT" %}
													Parent
												{% elseif role == "ROLE_ENSEIGNANT" %}
													Enseignant
												{% elseif role == "ROLE_ADMIN" %}
													Administrateur
												{% else %}

												{{role}}

												{% endif %}
											{% endfor %}
										</td>
										<td>{{ user.nom }}</td>
										<td>{{ user.prenom }}</td>
										<td>{{ user.telephone }}</td>
										<td>{{ user.sexe }}</td>
										<td>{{ user.nationalite }}</td>
										<td>{{ user.profession }}</td>
										<td class="py-0">
											  <img class="picSender" style="cursor: pointer; width:70px" src="{{ asset('assets/Uploaded/' ~ user.photo ) }}" data-toggle="modal" data-target="#exampleModalTooltip" data-toggle="tooltip" data-placement="right" title="See the picture" alt="photo Image">
									
										  </td>
										<td>
											<a href="{{ path('user_show', {'id': user.id}) }}" ><img src="{{ asset('assets/img/icons/voir.png') }}"></a>
								  			<a href="{{ path('user_modifier_utilisateur', {'id': user.id}) }}" class="btn btn-purple"><img src="{{ asset('assets/img/icons/modifier.png') }}"></a> 
											<a href="{{ path('user_supprimer', {'id': user.id}) }}" class="btn btn-purple"><img src="{{ asset('assets/img/icons/supprimer.png') }}"></a>
										</td>
									</tr>
								{% else %}
									<tr>
										<td colspan="5">Il n'y a pas d'utilisateur inscrit</td>
									</tr>
								{% endfor %}
							</tbody>
						</table>
						

				
					</div>
				</div>
		
			</div>
		
		</div>

        
    </main>
			{% include "menu/nav_footer/footer.html.twig" %}

		</div>
	</div>

	<script src="{{asset('assets/js/app.js')}} "></script>

</body>

</html>
